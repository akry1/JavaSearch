Static initializers are blocks of code that are executed at the same time as initializers for static fields. Static field initializers and static initializers are executed in the order declared. The static initialization is executed after the class is loaded.<br /><br /><br /><br />Code section 4.2: Static initializer.<br /><br /><br />1 static int count = 20;<br />2 static int[] squares;<br />3 static {  // a static initializer<br />4     squares = new int[count];<br />5     for (int i = 0; i < count; i++)<br />6         squares[i] = i * i;<br />7 }<br />8 static int x = squares[5];  // x is assigned the value 25